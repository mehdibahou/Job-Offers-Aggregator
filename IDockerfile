# Use a base image with Python, Selenium, and Edge Chromium WebDriver

FROM python:3.8-slim

# Install Selenium

RUN pip install selenium

# Install the Edge Chromium WebDriver using webdriver_manager

RUN pip install webdriver_manager

# Copy your Selenium scraping script into the container

COPY IndeedScraper.py /app/

# Set the working directory

WORKDIR /app/

# Define the command to run the scraping script

CMD ["python", "IndeedScraper.py"]
